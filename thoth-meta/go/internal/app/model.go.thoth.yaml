locator: internal/app/model.go
meta:
  function_list: []
  import_files_list:
    - path/filepath
  language: go
  purpose: "Defines CLI input/build models so command and engine layers share a stable contract."
  responsibilities: "Normalize and expose input path sets for downstream validation and execution."
  architecture_notes: "Model logic is intentionally thin to keep business rules in engine and command wiring in cli."

locator: internal/engine/catalog.go
meta:
  function_list:
    - BuildCatalog
    - ExplainKey
    - ExplainKeyTrace
  import_files_list:
    - cuelang.org/go/cue
    - cuelang.org/go/cue/cuecontext
    - fmt
    - github.com/flarebyte/ash-splash-norn/internal/app
    - github.com/flarebyte/ash-splash-norn/internal/diag
    - sort
  language: go
  purpose: "Builds list/explain-key views from registry CUE input for discoverability commands."
  responsibilities: "Decode registry capabilities, derive sorted catalog rows, and trace key derivation steps."
  architecture_notes: "Read-only query logic is separated from generation flows to keep command semantics predictable."

locator: internal/engine/preview.go
meta:
  function_list:
    - BuildPreview
  import_files_list:
    - cuelang.org/go/cue
    - cuelang.org/go/cue/cuecontext
    - fmt
    - github.com/flarebyte/ash-splash-norn/internal/app
    - github.com/flarebyte/ash-splash-norn/internal/diag
    - sort
  language: go
  purpose: "Builds deterministic dry-run preview rows from registry generation capabilities."
  responsibilities: "Decode registry capabilities, validate pattern policy, and return sorted preview records."
  architecture_notes: "Preview remains read-only and intentionally does not inspect config content beyond shared validation gates."

locator: internal/engine/lint_tokens.go
meta:
  function_list:
    - containsToken
    - extractPatternTokens
  import_files_list: []
  language: go
  purpose: "Provides lightweight token extraction helpers used by artifact-pattern policy checks."
  responsibilities: "Scan placeholder tokens and test token membership in pattern strings."
  architecture_notes: "Simple local helpers are kept separate to avoid coupling policy logic to parser details."

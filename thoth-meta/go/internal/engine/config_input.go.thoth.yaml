locator: internal/engine/config_input.go
meta:
  function_list:
    - readConfigInputSource
  import_files_list:
    - fmt
    - github.com/flarebyte/ash-splash-norn/internal/diag
    - os
    - path/filepath
    - sort
    - strings
  language: go
  purpose: "Loads config input from file or directory package with deterministic merge behavior."
  responsibilities: "Read CUE fragments, enforce single-package constraints, and emit stable layout diagnostics."
  architecture_notes: "Directory-package support is strict by design to avoid ambiguous compile units in automation."

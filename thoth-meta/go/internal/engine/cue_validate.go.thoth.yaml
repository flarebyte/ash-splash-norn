locator: internal/engine/cue_validate.go
meta:
  function_list:
    - ValidateCuePairs
    - extractPackage
    - mergeCueSources
    - mergeCueSourcesWithConfigDir
    - sanitizeCueErr
    - stripPackageDecl
    - validateCuePair
  import_files_list:
    - cuelang.org/go/cue
    - cuelang.org/go/cue/cuecontext
    - fmt
    - github.com/flarebyte/ash-splash-norn/internal/app
    - github.com/flarebyte/ash-splash-norn/internal/diag
    - os
    - regexp
    - strings
  language: go
  purpose: "Validates schema/input CUE pairs and shared merge semantics for engine consumers."
  responsibilities: "Merge schema and input sources, compile/validate via CUE, and emit stable schema diagnostics."
  architecture_notes: "Package matching and merge behavior are intentionally shared to prevent command-level divergence."
